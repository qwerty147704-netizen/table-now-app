# Table Now App - 사용 패키지 목록

이 문서는 `pubspec.yaml`에 정의된 모든 패키지와 그 용도를 설명합니다.

---

## 📦 프로덕션 의존성 (dependencies)

### Flutter 기본
- **flutter** (SDK)
  - Flutter 프레임워크 기본 패키지
  
- **flutter_localizations** (SDK)
  - 로케일 설정 (Google Maps 언어 설정용)
  - 다국어 지원을 위한 기본 패키지

### UI 아이콘
- **cupertino_icons** `^1.0.8`
  - iOS 스타일 아이콘 제공
  - Cupertino 디자인 시스템의 아이콘 사용

### 상태 관리
- **flutter_riverpod** `^3.1.0`
  - 상태 관리 패키지
  - 전역 상태 관리 및 의존성 주입에 사용

### 로컬 스토리지
- **get_storage** `^2.1.1`
  - GetStorage (GetX와 독립적으로 사용 가능한 스토리지 패키지)
  - 빠른 키-값 스토리지
  - 로그인 정보, 설정 등 저장에 사용

### 네트워크/API
- **http** `^1.1.0`
  - HTTP 클라이언트
  - NetworkUtil, AddressUtil에서 사용
  - REST API 통신에 사용

### 인증
- **google_sign_in** `6.2.1`
  - 구글 소셜 로그인
  - Google 계정으로 로그인 기능 제공

### Firebase
- **firebase_core** `^4.3.0`
  - Firebase 초기화
  - firebase_options.dart 사용 시 필요
  - Firebase 서비스의 기본 패키지

- **cloud_firestore** `^6.1.1`
  - Firestore 연동
  - NoSQL 데이터베이스 사용

- **firebase_messaging** `^16.1.0`
  - 푸시 알림 패키지
  - FCM (Firebase Cloud Messaging) 사용
  - 백그라운드/포그라운드 알림 처리

### 알림
- **flutter_local_notifications** `^19.5.0`
  - 로컬 알림 패키지
  - 포그라운드 알림 표시용
  - 앱이 실행 중일 때 알림 표시

### 지도/위치
- **google_maps_flutter** `^2.5.3`
  - 구글 맵 패키지
  - Google Maps를 Flutter 앱에 통합
  - 지도 표시 및 상호작용

- **geolocator** `^14.0.2`
  - 위치 정보 패키지
  - GPS 위치 정보 가져오기
  - 현재 위치 확인 및 위치 권한 처리

- **flutter_polyline_points** `^2.0.0`
  - Polyline 디코딩
  - 지도 경로 표시를 위한 Polyline 처리

### UI 컴포넌트
- **table_calendar** `^3.2.0`
  - 달력 패키지
  - 예약 날짜 선택 등에 사용

### 기기 정보
- **device_info_plus** `^10.1.0`
  - 기기 정보 확인
  - 기기 식별자용
  - 플랫폼, 모델, OS 버전 등 정보 조회

### 결제
- **tospayments_widget_sdk_flutter** `^2.1.2`
  - Toss 결제 SDK
  - Toss Payments 결제 기능 통합

### 보안/암호화
- **encrypt** `^5.0.3`
  - 데이터 암호화 패키지
  - 민감한 데이터 암호화/복호화

---

## 🛠️ 개발 의존성 (dev_dependencies)

### 스플래시 스크린
- **flutter_native_splash** `^2.4.7`
  - 네이티브 스플래시 스크린 생성
  - 앱 시작 시 표시되는 스플래시 화면 설정

---

## 📊 패키지 통계

- **총 프로덕션 패키지**: 18개
- **총 개발 패키지**: 1개
- **총 패키지 수**: 19개

---

## 📝 주요 사용 용도별 분류

### 핵심 기능
- **상태 관리**: flutter_riverpod
- **로컬 저장**: get_storage
- **네트워크**: http

### 사용자 인증
- **소셜 로그인**: google_sign_in
- **Firebase**: firebase_core, cloud_firestore, firebase_messaging

### 위치/지도
- **지도**: google_maps_flutter
- **위치**: geolocator
- **경로**: flutter_polyline_points

### 알림
- **푸시 알림**: firebase_messaging
- **로컬 알림**: flutter_local_notifications

### 결제
- **결제 SDK**: tosspayments_widget_sdk_flutter

### 보안
- **암호화**: encrypt

---

## 🔄 패키지 업데이트

패키지를 최신 버전으로 업데이트하려면:
```bash
flutter pub upgrade --major-versions
```

사용 가능한 업데이트 확인:
```bash
flutter pub outdated
```

---

**마지막 업데이트**: 2026-01-23
**Flutter SDK 버전**: ^3.10.1
